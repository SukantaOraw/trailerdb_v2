export type MovieCardProps = {
  title: string;
  year: string;
  rating: string;
  genre: string[];
};

export type Networks = {
  title: string;
  img: string;
};

export type SliderProps = {
  // id: string;
  title: string;
  year: string;
  rating: string;
  genre: string[];
  description: string;
  // backdrop: string;
  // poster: string;
};